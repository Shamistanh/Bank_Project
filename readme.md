

Simple BankApp is created in this Project. As a language Java (Spring Framework) has been used. Spring security used for login (as an admin or user)Currently, in memory user have been used, butthere is a code which you can find commented which with the help of that code you can get users from db, and add front pages to this project. For now, there is 2 kind of roles (USER and ADMIN). User can get all customers, check their number of delays and compare loan with their salary and send it to the manager(ADMIN) and admin can recieve all sent users, and decide by getting customers with theit id. Each 3 of attempt sent data shows error.


